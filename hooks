pipeline {
    agent { label 'slave-node' }
    stages {
        stage('test java installation') {
            steps {
                sh 'java -version'
                sh 'sleep 100'
            }
        }
    }
}
